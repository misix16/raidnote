html, body {
    margin: 0;
    padding: 0;
}

body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    line-height: 1.6;
    transition: background 0.2s, color 0.2s;
}

body.theme-dark {
    background: radial-gradient(circle at top, #0b1120, #020617 60%);
    color: #e5e7eb;
}

body.theme-light {
    background: #f4f4f5;
    color: #111827;
}

.header {
    position: sticky;
    top: 0;
    z-index: 20;
    backdrop-filter: blur(12px);
    background: rgba(15, 23, 42, 0.9);
    border-bottom: 1px solid #1f2937;
}

body.theme-light .header {
    background: rgba(255, 255, 255, 0.9);
    border-bottom-color: #e4e4e7;
}

.nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 10px 18px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
}

.nav-left {
    display: flex;
    flex-direction: column;
}

.logo {
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 18px;
    color: #38bdf8;
}

.logo span {
    color: #a855f7;
}

.nav-subtitle {
    font-size: 11px;
    color: #9ca3af;
}

body.theme-light .nav-subtitle {
    color: #6b7280;
}

.nav-center {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.nav-link {
    font-size: 14px;
    padding: 6px 10px;
    border-radius: 999px;
    text-decoration: none;
    color: #e5e7eb;
    background: transparent;
    border: 1px solid transparent;
    transition: background 0.1s, color 0.1s, border-color 0.1s;
}

.nav-link:hover {
    background: rgba(148, 163, 184, 0.18);
    border-color: rgba(148, 163, 184, 0.4);
}

body.theme-light .nav-link {
    color: #1f2937;
}

.nav-right {
    display: flex;
    align-items: center;
    gap: 8px;
}

.btn-ghost {
    border-radius: 999px;
    border: 1px solid #4b5563;
    background: transparent;
    color: #e5e7eb;
    padding: 6px 12px;
    font-size: 13px;
    cursor: pointer;
    transition: background 0.1s, border-color 0.1s, color 0.1s;
}

.btn-ghost:hover {
    background: rgba(148, 163, 184, 0.15);
    border-color: #9ca3af;
}

body.theme-light .btn-ghost {
    color: #111827;
    border-color: #d4d4d8;
}

body.theme-light .btn-ghost:hover {
    background: #e5e7eb;
}

.layout {
    max-width: 1200px;
    margin: 0 auto;
    padding: 18px;
    display: grid;
    grid-template-columns: minmax(0, 3fr) minmax(260px, 1.1fr);
    gap: 18px;
}

@media (max-width: 980px) {
    .layout {
        grid-template-columns: 1fr;
    }
}

.layout-main {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.layout-side {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.card {
    border-radius: 18px;
    padding: 18px 20px;
    border: 1px solid rgba(148, 163, 184, 0.35);
    background: radial-gradient(circle at top left, rgba(148, 163, 184, 0.1), rgba(15, 23, 42, 0.96));
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.35);
}

body.theme-light .card {
    background: #ffffff;
    border-color: #e5e7eb;
    box-shadow: 0 15px 35px rgba(15, 23, 42, 0.08);
}

.hero {
    padding-top: 22px;
    padding-bottom: 22px;
}

.hero-content {
    display: grid;
    grid-template-columns: minmax(0, 3fr) minmax(280px, 1.7fr);
    gap: 22px;
}

@media (max-width: 920px) {
    .hero-content {
        grid-template-columns: 1fr;
    }
}

.hero-text h1 {
    margin: 0 0 8px;
    font-size: 30px;
}

.hero-lead {
    margin: 0 0 14px;
    font-size: 15px;
    color: #d1d5db;
}

body.theme-light .hero-lead {
    color: #4b5563;
}

.hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.pill {
    padding: 4px 10px;
    border-radius: 999px;
    font-size: 12px;
    border: 1px solid rgba(148, 163, 184, 0.5);
    color: #e5e7eb;
    background: rgba(15, 23, 42, 0.8);
}

body.theme-light .pill {
    color: #111827;
    background: #f4f4f5;
    border-color: #d4d4d8;
}

.hero-panel {
    border-radius: 14px;
    padding: 14px;
    background: linear-gradient(145deg, rgba(15, 23, 42, 0.8), rgba(88, 28, 135, 0.9));
    border: 1px solid rgba(129, 140, 248, 0.8);
}

body.theme-light .hero-panel {
    background: linear-gradient(145deg, #f97316, #6366f1);
    color: #f9fafb;
}

.hero-panel-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 8px;
}

.hero-label {
    font-size: 14px;
    font-weight: 600;
}

.hero-mini {
    font-size: 11px;
    opacity: 0.7;
}

.hero-form {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 6px;
}

.field {
    display: flex;
    flex-direction: column;
    gap: 3px;
}

.field label {
    font-size: 12px;
}

.field input,
.field select {
    border-radius: 8px;
    border: 1px solid rgba(148, 163, 184, 0.7);
    padding: 6px 8px;
    font-size: 13px;
    background: rgba(15, 23, 42, 0.85);
    color: #e5e7eb;
}

body.theme-light .field input,
body.theme-light .field select {
    background: #f9fafb;
    color: #111827;
    border-color: #d4d4d8;
}

.field input:focus,
.field select:focus {
    outline: none;
    border-color: #38bdf8;
    box-shadow: 0 0 0 1px rgba(56, 189, 248, 0.6);
}

.btn-primary {
    border-radius: 999px;
    border: none;
    padding: 8px 12px;
    font-size: 13px;
    cursor: pointer;
    background: linear-gradient(135deg, #22c55e, #38bdf8);
    color: #0f172a;
    font-weight: 600;
    transition: transform 0.08s, box-shadow 0.08s;
}

.btn-primary:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 20px rgba(22, 163, 74, 0.35);
}

.suggestion-output {
    min-height: 38px;
    font-size: 13px;
    margin-top: 4px;
}

.section-header {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    align-items: baseline;
    margin-bottom: 8px;
}

.section-header h2 {
    margin: 0;
    font-size: 20px;
}

.section-filter {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
}

.section-description {
    font-size: 13px;
    color: #9ca3af;
}

body.theme-light .section-description {
    color: #4b5563;
}

.chip {
    border-radius: 999px;
    border: 1px solid rgba(148, 163, 184, 0.7);
    background: transparent;
    padding: 4px 10px;
    font-size: 12px;
    cursor: pointer;
    color: #e5e7eb;
}

body.theme-light .chip {
    color: #111827;
    border-color: #d4d4d8;
}

.chip-active {
    background: rgba(56, 189, 248, 0.12);
    border-color: #38bdf8;
}

.tabs {
    display: inline-flex;
    border-radius: 999px;
    padding: 3px;
    background: rgba(15, 23, 42, 0.8);
    margin-bottom: 12px;
}

body.theme-light .tabs {
    background: #e4e4e7;
}

.tab {
    border-radius: 999px;
    border: none;
    padding: 6px 14px;
    font-size: 13px;
    cursor: pointer;
    background: transparent;
    color: #9ca3af;
}

body.theme-light .tab {
    color: #6b7280;
}

.tab-active {
    background: rgba(15, 23, 42, 0.95);
    color: #e5e7eb;
}

body.theme-light .tab-active {
    background: #ffffff;
    color: #111827;
}

.tabs-content {
    margin-top: 8px;
}

.tab-panel {
    display: none;
}

.tab-panel-active {
    display: block;
}

.raid-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
    gap: 14px;
}

.raid-card {
    border-radius: 14px;
    padding: 10px 12px;
    background: rgba(15, 23, 42, 0.9);
    border: 1px solid rgba(55, 65, 81, 0.8);
    font-size: 13px;
}

body.theme-light .raid-card {
    background: #f9fafb;
    border-color: #e5e7eb;
}

.raid-card-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 4px;
}

.raid-card-header h3 {
    margin: 0;
    font-size: 16px;
}

.badge {
    border-radius: 999px;
    font-size: 11px;
    padding: 2px 7px;
    border: 1px solid;
}

.badge-performance {
    border-color: rgba(56, 189, 248, 0.9);
    color: #38bdf8;
}

.badge-safety {
    border-color: rgba(52, 211, 153, 0.9);
    color: #4ade80;
}

.badge-balanced {
    border-color: rgba(249, 115, 22, 0.9);
    color: #fdba74;
}

.badge-hybrid {
    border-color: rgba(168, 85, 247, 0.9);
    color: #c4b5fd;
}

.raid-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    font-size: 11px;
    margin-top: 6px;
    color: #9ca3af;
}

body.theme-light .raid-meta {
    color: #6b7280;
}

.diagram-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
    gap: 14px;
}

.diagram-card {
    border-radius: 14px;
    padding: 10px 12px;
    background: rgba(15, 23, 42, 0.97);
    border: 1px dashed rgba(148, 163, 184, 0.7);
}

body.theme-light .diagram-card {
    background: #ffffff;
    border-style: solid;
    border-color: #e4e4e7;
}

.diagram-title {
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 4px;
}

.diagram {
    font-family: "JetBrains Mono", "Fira Code", Menlo, monospace;
    font-size: 12px;
    margin: 0;
    white-space: pre;
    overflow-x: auto;
}

.accordion {
    border-radius: 10px;
    border: 1px solid rgba(55, 65, 81, 0.8);
    overflow: hidden;
}

body.theme-light .accordion {
    border-color: #e5e7eb;
}

.accordion-item + .accordion-item {
    border-top: 1px solid rgba(55, 65, 81, 0.7);
}

body.theme-light .accordion-item + .accordion-item {
    border-top-color: #e5e7eb;
}

.accordion-header {
    width: 100%;
    padding: 8px 10px;
    border: none;
    background: rgba(15, 23, 42, 0.9);
    color: inherit;
    display: flex;
    justify-content: space-between;
    cursor: pointer;
    font-size: 14px;
}

body.theme-light .accordion-header {
    background: #f9fafb;
}

.accordion-body {
    display: none;
    padding: 8px 10px 10px;
    background: rgba(15, 23, 42, 0.96);
    font-size: 13px;
}

body.theme-light .accordion-body {
    background: #ffffff;
}

.accordion-body-open {
    display: block;
}

.two-col {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 10px;
}

.list-good {
    padding-left: 18px;
    margin: 0;
}

.list-bad {
    padding-left: 18px;
    margin: 0;
}

.list-good li {
    margin-bottom: 4px;
}

.list-bad li {
    margin-bottom: 4px;
}

.raid-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
}

.raid-table th,
.raid-table td {
    padding: 8px 8px;
    text-align: left;
}

.raid-table thead {
    background: rgba(15, 23, 42, 0.95);
}

body.theme-light .raid-table thead {
    background: #f9fafb;
}

.raid-table tbody tr:nth-child(odd) {
    background: rgba(15, 23, 42, 0.8);
}

.raid-table tbody tr:nth-child(even) {
    background: rgba(15, 23, 42, 0.92);
}

body.theme-light .raid-table tbody tr:nth-child(odd) {
    background: #ffffff;
}

body.theme-light .raid-table tbody tr:nth-child(even) {
    background: #f4f4f5;
}

.table-wrapper {
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid rgba(148, 163, 184, 0.5);
}

body.theme-light .table-wrapper {
    border-color: #e4e4e7;
}

.scenario-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
    gap: 10px;
    margin-top: 8px;
}

.scenario {
    border-radius: 12px;
    padding: 10px;
    border: 1px solid rgba(148, 163, 184, 0.7);
    background: rgba(15, 23, 42, 0.92);
    text-align: left;
    cursor: pointer;
    transition: transform 0.08s, box-shadow 0.08s, border-color 0.08s;
}

body.theme-light .scenario {
    background: #ffffff;
    border-color: #e5e7eb;
}

.scenario:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 25px rgba(15, 23, 42, 0.55);
    border-color: #38bdf8;
}

.scenario-title {
    font-weight: 600;
}

.scenario-text {
    font-size: 12px;
    color: #9ca3af;
}

body.theme-light .scenario-text {
    color: #6b7280;
}

.scenario-output {
    margin-top: 10px;
    font-size: 13px;
}

.lab-layout {
    display: grid;
    grid-template-columns: minmax(220px, 1.3fr) minmax(0, 2fr);
    gap: 14px;
    margin-top: 10px;
}

@media (max-width: 820px) {
    .lab-layout {
        grid-template-columns: 1fr;
    }
}

.lab-form {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.lab-result {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.lab-result-main {
    border-radius: 12px;
    padding: 10px;
    background: rgba(15, 23, 42, 0.95);
    border: 1px solid rgba(55, 65, 81, 0.9);
}

body.theme-light .lab-result-main {
    background: #ffffff;
    border-color: #e5e7eb;
}

.lab-result-row {
    display: flex;
    justify-content: space-between;
    font-size: 13px;
}

.lab-bar {
    height: 20px;
    border-radius: 999px;
    overflow: hidden;
    background: rgba(15, 23, 42, 0.8);
    border: 1px solid rgba(55, 65, 81, 0.9);
}

body.theme-light .lab-bar {
    background: #e5e7eb;
    border-color: #d4d4d8;
}

.lab-bar-data {
    height: 100%;
    background: linear-gradient(90deg, #22c55e, #38bdf8);
    width: 0;
    transition: width 0.2s;
}

.lab-bar-overhead {
    height: 100%;
    background: linear-gradient(90deg, #ef4444, #f59e0b);
    width: 0;
    transition: width 0.2s;
}

.lab-caption {
    font-size: 11px;
    color: #9ca3af;
}

body.theme-light .lab-caption {
    color: #6b7280;
}

.lab-error {
    min-height: 16px;
    font-size: 12px;
    color: #fecaca;
}

body.theme-light .lab-error {
    color: #b91c1c;
}

.side-card h3 {
    margin-top: 0;
    margin-bottom: 8px;
    font-size: 16px;
}

.toc {
    list-style: none;
    padding-left: 0;
    margin: 0;
    font-size: 13px;
}

.toc li + li {
    margin-top: 4px;
}

.toc a {
    text-decoration: none;
    color: inherit;
}

.toc a:hover {
    text-decoration: underline;
}

.side-text {
    font-size: 13px;
}

.quiz-progress {
    margin-top: 10px;
    height: 6px;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.85);
    border: 1px solid rgba(55, 65, 81, 0.9);
    overflow: hidden;
}

body.theme-light .quiz-progress {
    background: #e5e7eb;
    border-color: #d4d4d8;
}

.quiz-progress-bar {
    height: 100%;
    width: 0;
    background: linear-gradient(90deg, #22c55e, #38bdf8);
    transition: width 0.2s;
}

#quiz-container {
    font-size: 13px;
}

.quiz-question {
    margin-bottom: 8px;
}

.quiz-option {
    display: block;
    width: 100%;
    text-align: left;
    border-radius: 10px;
    border: 1px solid rgba(75, 85, 99, 0.9);
    background: rgba(15, 23, 42, 0.95);
    padding: 6px 8px;
    margin-bottom: 6px;
    cursor: pointer;
    transition: background 0.1s, border-color 0.1s, transform 0.08s;
}

body.theme-light .quiz-option {
    background: #f9fafb;
    border-color: #e5e7eb;
}

.quiz-option:hover {
    transform: translateY(-1px);
    background: rgba(31, 41, 55, 0.9);
}

body.theme-light .quiz-option:hover {
    background: #e5e7eb;
}

.quiz-option-correct {
    border-color: rgba(34, 197, 94, 0.9);
    background: rgba(22, 163, 74, 0.25);
}

.quiz-option-wrong {
    border-color: rgba(248, 113, 113, 0.9);
    background: rgba(239, 68, 68, 0.2);
}

.quiz-feedback {
    min-height: 18px;
    font-size: 12px;
}

.quiz-feedback-ok {
    color: #4ade80;
}

.quiz-feedback-err {
    color: #fecaca;
}

body.theme-light .quiz-feedback-err {
    color: #b91c1c;
}
